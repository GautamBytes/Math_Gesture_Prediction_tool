opencv-python #Captures and processes video frames using OpenCV.
cvzone #Simplifies hand tracking and gesture detection with a wrapper over MediaPipe.
numpy #Handles array operations for image and canvas manipulation.
google-generativeai # Sends prompts and images to Google Gemini AI for responses.
Pillow #Converts and processes image formats (e.g., NumPy to grayscale).
streamlit #Builds the interactive web app interface for video and output display.
imageio #Lists and accesses available webcam devices.
mediapipe #Provides real-time hand landmark detection (used internally by cvzone).
python-dotenv #Loads API keys and config variables from a .env file.
